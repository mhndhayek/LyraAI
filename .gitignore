# dependencies and build output
node_modules/
dist/
# the voice engine's Python environment (npm run voice:setup)
voice/.venv/
# OS and editor noise
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
# running gitleaks by hand leaves its binary here, and a report it writes can
# quote the secret it found, so neither may be committed
gitleaks
*.sarif

# dependencies and build output
node_modules/
dist/
# the voice engine's Python environment (npm run voice:setup)
voice/.venv/
# OS and editor noise
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
# scan reports, if you run the security scanners by hand — a gitleaks report can
# quote the secret it found, so it must never be committed
*.sarif
gitleaks
